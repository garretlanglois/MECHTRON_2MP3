//Matrix Multiplication

#include <stdio.h>
#include <stdlib.h>


void matrixMult(int m, int n, int p, int A[m][n], int B[n][p]) {
    for (int i = 0; i < ) {

    }
}

int main(void) {
    const int m = 2;
    const int n = 3;
    const int p = 2;

    int **A = malloc(m * sizeof(int*));
    int **B = malloc(n * sizeof(int*));

    int **C = malloc(m*sizeof(int*));

    int A[m][n] = {{1, 5, 3},{2, 6, 3}};

    int B[n][p] = {{1, 5},{9, 2},{7, 1}};



}